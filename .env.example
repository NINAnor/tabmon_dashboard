# Base data directory URL for rclone service
BASE_DATA_DIR=http://rclone:8081/data/

# Authentication credentials for accessing protected data
# These should match the credentials in your htpasswd file
# In production, upload the .env file as a secret in Portainer
AUTH_USERNAME=your_username_here
AUTH_PASSWORD=your_password_here
